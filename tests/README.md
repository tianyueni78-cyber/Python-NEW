# Tests

Tests will enforce the seven validation gates in `AGENTS.md`.

The highest-priority checks are fixed-input MATLAB-Python comparisons for data
loading, chromosome handling, decoding, objective values, multi-objective
operators, Q-learning behavior, metrics, and dynamic-event feasibility.
