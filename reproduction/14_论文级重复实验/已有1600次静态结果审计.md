# 已有1600次静态结果审计

## 原始位置

- 目录：`D:\CODEX\Python-NEW-step14-results\static_v2`
- Git提交：`a02dd739dbd7c179d3078ebb0f6f81a99c4740df`
- 成功：1600
- 失败：0
- 八种算法各200次。

该目录不可覆盖或改名，继续作为统一机器能耗口径的完整原始证据保存。

## 可复用范围

已有结果与MATLAB原样口径一致的算法：

- QNSGA-II：200次；
- 消融B：200次；
- 消融C：200次；
- 消融full：200次。

共800次，无需重复运行。

必须在新目录重跑的算法：

- NSGA-II：200次；
- MOEA/D：200次；
- MOPSO：200次；
- 消融A：200次。

共800次。原因是旧Python统一使用最后加工完成时间和机器能耗；MATLAB对应目录启用成品返回卸载站，并以机器能耗与AGV能耗之和作为第二目标。

## 预算复用

三个对比算法仍使用已有QNSGA-II对应实例、重复编号的实际耗时作为时间预算。新增运行器参数`--budget-source`只读取旧目录的成功记录，不修改旧目录。

## 待处理的计时污染

旧QNSGA-II的200条耗时中，199条均不超过600秒，中位数为86.28秒、99%分位数为180.78秒；仅`Mk09`第11次重复记录为14993.82秒。该值与电脑休眠时段一致，属于墙钟计时被系统休眠污染，不代表算法实际计算时间。

在用户批准处理口径前，不修改该原始记录，也不静默截断预算。建议仅以相同实例、种子和QNSGA-II配置重新运行一次计时校准，并让该组的NSGA-II、MOEA/D和MOPSO读取校准预算；原始结果继续完整保留。
