# 第12步完成报告

## 完成范围

完成 HV、IGD、Spacing、C-metric、RSI 的 MATLAB 固定输入对照，实现批量实验、配对随机种子、原始 Pareto 解、染色体、曲线、运行时、软件与代码版本、配置和输入哈希、失败状态、指标汇总和绘图数据的可追溯记录。未进入第13步。

## MATLAB参考

固定目标矩阵通过原 HV、IGD 和 C 调用链导出；Spacing 由于环境缺少 `pdist2` 工具箱，验证脚本展开原始公式。参考文件为 `python_baseline/data/matlab_reference/metrics_step12.json`。

## Python实现

- `python_baseline/dfjspt/metrics.py`：论文指标和动态 RSI 分量。
- `python_baseline/dfjspt/experiments.py`：统一运行、记录、汇总和绘图数据导出。
- `scripts/run_experiments.py`：JSON 配置批处理入口。
- `scripts/check_step12.py`：第12步独立验收。

## 验收结果

- MATLAB 固定参考与 Python 逐项一致。
- 第12步专项测试：9/9通过。
- 全量测试：81/81通过。
- 最终小规模验收：8个独立算法/消融入口全部成功，配对种子一致。
- Gate 1—6 无回归。
- 快照 `.py` 文件与权威代码 SHA-256 一致。

## 边界

完整 baseline 尚未成立。第13—15步、论文规模重复实验和 Gate 7 仍未完成。
