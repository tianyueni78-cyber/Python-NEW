# 第10步完成报告

## 一、完成范围

第10步“实现消融版本”已完成，权威实现提交为`2f72d70d34b640f7fd2564a09d353dffe55a4902`。

已实现并提供统一入口的四个版本：

- A：普通随机初始化的基础NSGA-II；
- B：混合初始化、无局部搜索；
- C：混合初始化、随机N1—N6、无Q更新；
- `full`：混合初始化、Q-learning选择N1—N6并更新Q表。

## 二、实现方式

A复用第9步独立NSGA-II入口。B、C和完整版本通过`qnsga2.py`的明确具名模式共享真正相同的主循环。`run_qnsga2`默认模式仍为`full`，并有回归测试证明默认调用与显式`full`结果完全相同。

`ablations.py`集中记录四版本特征矩阵，但没有把MOEA/D、MOPSO或动态算法并入该配置。命令行入口为`scripts/run_ablation.py`。

## 三、MATLAB固定参考

验证脚本`tests/matlab_reference/export_ablation_reference.m`只读取四个锁定原入口，导出了初始化类型、初始排序、局部搜索、邻域数量、随机动作、Q动作、Q更新和停止方式。Python模块特征与MATLAB参考逐项一致；停止条件按公平消融统一为相同代数，并已登记源码漂移。

## 四、验收结果

- 第10步新增测试：6项；
- 全量测试：67项，失败0项；
- 四版本固定种子重复运行：结果可重复且染色体合法；
- A、B、C的Q表保持全零；完整版Q表实际更新；
- C和完整版每代对每个种群个体执行一次邻域评价；
- Gate 1—5及第9步验收保持通过；
- Python编译和Git差异检查通过；
- 5个Python权威文件与步骤快照SHA-256全部一致。

## 五、当前状态

第1—10步完成，第11—15步尚未执行。本步没有实现动态事件、论文指标或论文规模重复实验，因此当前仍不能宣称完整baseline成立。
