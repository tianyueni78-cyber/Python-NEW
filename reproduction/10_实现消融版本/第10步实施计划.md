# 第10步实施计划

## 任务1：固定MATLAB特征参考

1. 新建`tests/matlab_reference/export_ablation_reference.m`，只读取四个原入口文本。
2. 固定导出四版本的初始化、初始排序、局部搜索、动作、Q更新、停止方式和邻域数量。
3. 运行MATLAB生成`python_baseline/data/matlab_reference/ablations_step10.json`。

## 任务2：先写失败测试

1. 新建`tests/test_step10_ablations.py`。
2. 测试Python具名特征与MATLAB固定参考一致。
3. 测试默认完整QNSGA-II与显式完整模式完全一致。
4. 测试A/B/C/完整版本固定种子可重复、输出合法、评价次数符合模块开关、仅完整版本更新Q表。
5. 运行目标测试并确认因接口尚不存在而失败。

## 任务3：最小实现

1. 修改`python_baseline/dfjspt/qnsga2.py`，增加默认不变的显式模式分支。
2. 新建`python_baseline/dfjspt/ablations.py`，定义四个枚举和统一结果。
3. 新建`scripts/run_ablation.py`和`scripts/check_step10.py`。
4. 逐项运行第10步测试直至通过，不重构无关模块。

## 任务4：验收归档

1. 运行四个小规模入口并保存样例结果。
2. 重跑Gate 1—5、第9步验收、全量测试和编译检查。
3. 生成中文差异记录、验收证据和完成报告。
4. 将本步所有新增或修改`.py`复制到`reproduction/10_实现消融版本/Python代码/`并核对SHA-256。
5. Git差异检查通过后提交、推送`main`并核对远端哈希。
