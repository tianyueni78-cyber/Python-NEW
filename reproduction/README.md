# Reproduction evidence

This directory is the audit trail for baseline validity.

Planned records:

- paper-section to MATLAB-function to Python-module source map;
- executable MATLAB call graph;
- parameter and instance inventory;
- discrepancy ledger;
- validation-gate evidence;
- final baseline acceptance report.

Every unresolved discrepancy must include its potential baseline impact.
