# 第09步完成报告

## 一、完成范围

第9步“实现三种对比算法”已完成，权威实现提交为`21b79708d40ecd4ff99b2b8144433551da7ed6fc`。

已新增三个相互独立的优化器和命令行入口：

- NSGA-II：普通随机初始化、锦标赛、IPOX/MPX、变异、非支配环境选择；
- MOEA/D：线性权重、权重邻域、倒数权重切比雪夫、0.5评价跳过、邻域替换；
- MOPSO：连续粒子编码、外部网格档案、领导者选择、速度位置更新、三分组变异、PBEST更新和N1—N3 VNS。

本步未进入第10步消融版本，也未运行论文规模统计实验。

## 二、MATLAB证据

已完整检查`dif_main.m`到三个算法入口及各自直接辅助函数的调用链。新增验证脚本`tests/matlab_reference/export_comparators_reference.m`，在不修改原源码的前提下生成`comparators_step9.json`。Python与固定参考在MOEA/D权重、邻域、切比雪夫、邻域替换、MOPSO连续编码映射和支配判定上相符。

## 三、目标与预算

三个Python入口统一调用Gate 3权威静态解码和目标，避免不同算法在不同makespan与能耗定义下比较。三个入口均支持固定代数和CPU时间上限；论文实验应把同一次QNSGA-II运行时间传给三个对比算法。对比目录自身的目标定义差异已写入差异记录，未静默隐藏。

## 四、验收结果

- 第9步新增测试：11项；
- 全部测试：61项，失败0项；
- 三个小规模命令行入口：均可运行并输出合法Pareto染色体；
- `scripts/check_step9.py`：通过；
- Gate 1—5：保持通过；
- Python编译检查：通过；
- 8个步骤快照Python文件：SHA-256全部一致；
- Git差异格式检查：通过。

## 五、当前项目状态

第1—9步完成；第10—15步仍待执行。当前只能说明第9步对比算法实现完成，不能宣称完整baseline已经成立。完整baseline仍需消融、三类动态重调度、指标与实验系统、逐层一致性、论文规模重复实验以及最终冻结。
