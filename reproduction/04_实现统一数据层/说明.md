# 第04步：实现统一数据层

状态：已完成。进入第5步前，必须保持 `Gate1_输入一致性证据.json` 的结论为“通过”。

交付内容：

1. `python_baseline/dfjspt/data.py`：统一数据模型、Brandimarte 读取、校验和资源装配。
2. `python_baseline/dfjspt/convert_resources.py`：从原机器工作簿转换距离与能耗。
3. `python_baseline/data/brandimarte/`：Mk01—Mk10 固定输入。
4. `python_baseline/data/matlab_reference/`：由原 `benchmarkRead.m` 生成的参照。
5. `tests/test_gate1_input.py` 与 `scripts/check_gate1.py`：自动判定 Gate 1。
6. `数据层规格与差异.md`：字段语义、转换规则和已发现源码问题。
