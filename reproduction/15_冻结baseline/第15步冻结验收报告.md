# 第15步冻结验收报告

## 当前判定

论文统计复现意义上的`baseline-v1.0`不创建。经用户批准，当前版本降级冻结为`baseline-matlab-observed-v1.0`，其含义严格限定为：**现存MATLAB活动代码的可执行Python重建**。

该标签用于保护现有代码、1600次静态结果、400次动态结果及全部差异证据，便于后续创新采用固定参照；它不表示论文统计结论已经复现，也不表示Gate 7科学验收通过。

## 七个Gate状态

| Gate | 状态 | 说明 |
|---|---|---|
| Gate 1 输入一致 | 通过 | 统一检查通过 |
| Gate 2 染色体合法 | 通过 | 统一检查通过 |
| Gate 3 解码与目标一致 | 通过 | 固定染色体证据通过 |
| Gate 4 多目标算子一致 | 通过 | 固定输入证据通过 |
| Gate 5 Q-learning行为 | 通过 | 状态、动作、奖励、更新和消融测试通过 |
| Gate 6 动态事件有效 | 通过 | 三类事件及策略合法性测试通过 |
| Gate 7 统计复现 | **不通过** | 运行完整，但静态及动态核心统计结论未复现或不可公平比较 |

## 验收命令结果

- `python scripts/check_step13.py`：Gate 1至6及指标检查全部通过；
- `python -m unittest discover -s tests -v`：101项通过；
- `python scripts/check_gate7.py ...`：文件完整性检查通过，1600次静态和400次动态均齐全；
- 科学结论审计：不通过。

## 未解决项

1. 需要正式MATLAB实验输出或作者保存的工作区，核定动态三类事件实际调用路径、TEC口径和每次10解的选择方式；
2. 静态目标口径不统一、C优于full以及动态RS结论未总体复现，均保留为公开差异；
3. 若未来取得新证据，可另行完成Gate 7，再决定是否发布论文统计复现意义上的`baseline-v1.0`。

当前降级冻结不需要再次运行实验，也不得回写或美化现有结果。

## 冻结边界

- 固定代码提交：以标签实际指向的Git提交为准；
- 固定本地结果：静态与动态目录及第14步证据中记录的SHA-256；
- 后续创新必须使用独立算法、配置或分支，不反向修改本标签；
- 允许作为工程和算法开发基线使用；引用时必须注明“MATLAB活动代码重建，论文统计结论未完全复现”。
